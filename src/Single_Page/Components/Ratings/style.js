import styled from 'styled-components';

const RatingContainer = styled.div`
  margin: 0 0.5rem;
`;
const StyledHeartsIcon = styled.i`
  color: var(--darkred);
`;

export { StyledHeartsIcon, RatingContainer };
