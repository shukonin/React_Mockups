import styled from 'styled-components';

const StyledLeftParagraph = styled.p`
  color: var(--darkblue);
  margin: 1rem 0;
`;

const StyledNoteParagraph = styled.p`
  color: var(--darkblue);
  font-size: 0.8rem;
`;

const StyledSquareCardParagraph = styled.p`
  font-size: 0.8rem;
`;

const StyledCommunityCardParagraph = styled.p`
  font-size: 0.8rem;
  color: var(--red);
`;

const StyledSpan = styled.span`
  color: var(--red);
  font-weight: 800;
`;

export {
  StyledLeftParagraph,
  StyledNoteParagraph,
  StyledSquareCardParagraph,
  StyledCommunityCardParagraph,
  StyledSpan,
};
